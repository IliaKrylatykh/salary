import classes from './mainHeader.module.scss';

const MainHeader = () => {
    return (
        <div className={classes.mainHeader}>
            Company name
        </div>
    );
};

export default MainHeader;